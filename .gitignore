# OS / editor cruft — never publish
.DS_Store
Thumbs.db
desktop.ini
*.swp

# Generated by build/generate.py from the feed export — never hand-edited,
# and never committed: the current output is built from SYNTHETIC sample data.
# Regenerate from the real feed at deploy time.
p/
