User-agent: *
Allow: /

# GitHub Pages serves the entire repo (there is a .nojekyll file). Tooling and
# fixtures moved to the private app repo (app/site-build/, 2026-08-16); the
# README is the one non-page file left here. Keep it out of search results.
Disallow: /README.md

# Two sitemaps: the hand-maintained top-level pages, and the permalink pages
# under /p/ — generated at deploy by build/generate.py, whose own sitemap lists
# every post page (the root sitemap does not enumerate them). Without this line
# the /p/ pages are crawl-orphans.
Sitemap: https://billtracking.org/sitemap.xml
Sitemap: https://billtracking.org/p/sitemap.xml
