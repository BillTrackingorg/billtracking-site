User-agent: *
Allow: /

# GitHub Pages serves the entire repo (there is a .nojekyll file). The permalink
# generator (tools/) and the README are the only non-page files here — keep
# them out of search results. (Other tooling lives in the private app repo.)
Disallow: /README.md
Disallow: /tools/
Disallow: /.github/

# Two sitemaps: the hand-maintained top-level pages, and the permalink pages
# under /p/ — rendered every 2 h by the app repo's permalinks workflow, whose own sitemap lists
# every post page (the root sitemap does not enumerate them). Without this line
# the /p/ pages are crawl-orphans.
Sitemap: https://billtracking.org/sitemap.xml
Sitemap: https://billtracking.org/p/sitemap.xml
